var check = require('./globals/globals.js')
var mysql = require('mysql');

module.exports={
    login : function (browser) {
      browser
      var user = check.connection
      var userConnection=mysql.createConnection(user)
      browser
    userConnection.connect();

    userConnection.query('select * from first_table;', function(err, rows, fields) {
      if (!err)
        console.log('The solution is: ', rows.length);
      else
        console.log('Error while performing Query.');
    });

  userConnection.end();

}

}