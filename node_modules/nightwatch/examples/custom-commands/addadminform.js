exports.command=function(){
	this
	.useXpath()
	 .click("//*[@id='adminDefaultFormsContent']/div[1]/span[5]/button")
         .useCss()
         .waitForElementVisible('#myModalLabel5' , 60000)
         .pause(1000)
           this
            .useXpath()
         	
         .getText("//*[@id='tblGrid']/tbody/tr[1]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[1]/td[2]/button")
                  .pause(1000)
              }
  })
        .pause(500)
        .getText("//*[@id='tblGrid']/tbody/tr[2]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[2]/td[2]/button")
                  .pause(1000)
              }
  })
.pause(500)
        .getText("//*[@id='tblGrid']/tbody/tr[3]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[3]/td[2]/button")
                  .pause(1000)
              }
  })
.pause(500)


  .getText("//*[@id='tblGrid']/tbody/tr[4]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[4]/td[2]/button")
                  .pause(1000)
              }
  })

.pause(500)
        .getText("//*[@id='tblGrid']/tbody/tr[5]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[5]/td[2]/button")
                  .pause(1000)
              }
  })

.pause(500)
        .getText("//*[@id='tblGrid']/tbody/tr[6]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[6]/td[2]/button")
                  .pause(1000)
              }
  })
   .pause(500)
           .getText("//*[@id='tblGrid']/tbody/tr[7]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[7]/td[2]/button")
                  .pause(1000)
              }
  })


.pause(500)
        .getText("//*[@id='tblGrid']/tbody/tr[8]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[8]/td[2]/button")
                  .pause(1000)
              }
  })

.pause(500)
       .getText("//*[@id='tblGrid']/tbody/tr[9]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[9]/td[2]/button")
                  .pause(1000)
              }
  })
   
.pause(500)
       .getText("//*[@id='tblGrid']/tbody/tr[10]/td[2]/button", function(result) {
              if(result.value=="Add to patient file"){
                this
                 .useXpath()
                   .click("//*[@id='tblGrid']/tbody/tr[10]/td[2]/button")
                  .pause(1000)
              }
  })
     this
      .pause(500)
      .useXpath()
      .click("//*[@id='addRemoveFormsModal']/div/div/div[3]/button")



     }