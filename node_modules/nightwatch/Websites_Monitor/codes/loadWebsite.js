var urlData = require('../data/url.json')
module.exports = {

    'Login': function (client) {
        client
           .windowMaximize();
        var date =new Date();
        var print=date.getDate()+""+(date.getMonth()+1)+""+date.getFullYear()+"_"+date.getHours()+""+date.getMinutes();

           // .waitForElementVisible('.home-bulle', 20000)
        for (var i = 0; i < urlData.url.length; i++) {
            client
                .url(urlData.url[i]);
        console.log(urlData.url[i]);
                //.login(Data.user1[0].username, Data.user1[0].password)
            client.waitForElementPresent('button[type="submit"]', 60000);
            //client.pause(3000);

//Save Screenshot


             var save_directory = 'Websites_Monitor/screenshot/'+print;

             //var name=urlData.url[i].replace(/./g, "_");
             client
             .saveScreenshot(save_directory+'/'+""+urlData.url[i]+'.jpg')

        }
    }
}
